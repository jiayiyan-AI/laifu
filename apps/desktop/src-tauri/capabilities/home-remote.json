{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "home-remote",
  "description": "home 窗口加载的是远程 web 首页（不是 bundled 前端资源），Tauri 默认不放行远程 origin 调命令；这里显式允许该 origin 调 open_oauth_in_browser（Google OAuth 走系统浏览器）与 download_cloud_file（云盘文件走原生「另存为」），见 app/auth_commands.rs。",
  "windows": ["home"],
  "remote": {
    "urls": ["http://localhost:3000/*", "https://app-lingxi-dev-gateway.azurewebsites.net/*", "https://laifu.uncagedai.org/*"]
  },
  "permissions": ["allow-open-oauth-in-browser", "allow-download-cloud-file"]
}
