{
  "name": "lingxi",
  "version": "0.0.1",
  "private": true,
  "description": "灵犀 — 数字员工平台 monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
