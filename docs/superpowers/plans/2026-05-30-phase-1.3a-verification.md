# Phase 1.3a 本地验证记录

- 验证时间：2026-05-30
- 验证环境：macOS Darwin 25.5.0, Node 24, Supabase local (project_id=laifu)
- AUTH_MODE=dev, PROVISIONER=local

## DoD 项

- [x] `pnpm -r test` — 50 tests pass (15 from 1.2 + 35 new from 1.3a)
- [x] `pnpm -r lint` — clean
- [x] stub-container 起步并响应 SSE
- [x] Dev login (`POST /api/auth/dev/login`) 拿到 session cookie
- [x] `/api/auth/me` 用 cookie 返回用户信息
- [x] 已迁移 endpoints 不再接受 x-user-id，无 cookie 一律 401
- [x] Threads CRUD 全部按预期工作（POST/GET 列表/GET 单个/DELETE 软删）
- [x] Chat 双点流跑通：POST /start → outer stream_id → GET /stream → SSE 字节流原样到 client
- [x] `POST /api/auth/logout` 清 cookie，之后 /me 返 401

## E2E 实跑日志摘要

```
1. dev login → user_id=b538a7ef-... + lingxi_sid cookie
2. /me → {wx_unionid: 'wx_e2e_test_001', nickname: 'E2E用户'}
3. purchase → status='provisioning'
4. status 6 steps: 5% → 20% → 40% → 70% → 90% → 100% (ready)
5. POST /api/threads → id='thr_mps2elevylan'
6. GET /api/threads → [thr_mps2elevylan]
7. POST /api/chat/start {thread_id, message} → stream_id='stm_mps2es...'
8. GET /api/chat/stream → 35 token events + 1 tool event + 1 done event
   • full_reply 完整透传到 client
   • session_id='web:thr_mps2elevylan' 来自 stub container 的响应
9. 401 checks: 无 cookie 访问 /threads、/purchase 都返 401
10. logout → {ok:true} → 之后 /me 返 401
```

## 涉及组件

- @lingxi/stub-container (新)：模拟 Hermes Container HTTP 契约
- @lingxi/gateway (扩展)：auth/ + chat/ + api/{threads,chat}.ts
- @lingxi/shared (扩展)：DevLoginRequest / ThreadCreate* / WebChat* 契约
- 本地 Supabase (laifu)：users / container_mapping / threads 表

## 验收结论

**Phase 1.3a 本地验收通过。** 后端 (OAuth + sessions + threads + chat SSE) 完整可用。
下一步进入 Phase 1.3b（React + Vite 前端），所有 API 契约已固定。
