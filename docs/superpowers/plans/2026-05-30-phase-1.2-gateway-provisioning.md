# 灵犀 MVP — Phase 1.2 Implementation Plan：Gateway + Provisioning + Azure 基础设施

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up monorepo 骨架、Gateway Node 进程、Supabase 表、Azure 基础设施和异步 provisioning 流程。**完成标志**：Postman 调 `POST /api/purchase`，能在 30 秒～3 分钟内观察到 `container_mapping` 状态从 `provisioning` → `ready`，并在 Azure Portal 看到一个新的 Container App 被创建。

**Architecture:** Monorepo 用 pnpm workspaces；Gateway 用 Express + TypeScript，业务模块化（api / db / provisioning）；Supabase 作 PostgreSQL；Azure SDK 用 `@azure/arm-appcontainers` 异步创建 ContainerApp，靠 `pollUntilDone` 等就绪。Provisioning 在 Gateway 进程内 fire-and-forget Promise 跑，状态以 DB 为 source of truth，启动时扫表续追。

**Tech Stack:** Node 24, TypeScript 5.x, Express 4, Vitest, supertest, @supabase/supabase-js 2.x, @azure/arm-appcontainers, @azure/arm-storage, @azure/identity, pnpm workspaces, Bicep.

**Spec:** [docs/superpowers/specs/2026-05-30-lingxi-mvp-spec.md](../specs/2026-05-30-lingxi-mvp-spec.md)（Phase 1.2 在 §11.1.2）。

**Prerequisites（开工前请确保已就绪）：**
- Azure 订阅（有创建 Resource Group / Container Apps / Storage / Container Registry / App Service 的权限）
- Supabase 账号（free tier 即可）
- 本地工具：pnpm ≥ 9、Node ≥ 24、Azure CLI（`az --version` 可以跑）、Supabase CLI（`supabase --version`）
- （可选）Postman 或 `curl` 用于手动测试

**这个 plan 的范围**：MVP Phase 1.2。Phase 1.3（Web 端到端）、1.4（微信端到端）、1.5（部署）会在 1.2 落地后**单独**写 plan。

---

## File Structure

执行本 plan 完成后，repo 状态：

```
laifu/
├── .gitignore                                    # NEW
├── .npmrc                                        # NEW
├── package.json                                  # NEW (root workspace)
├── pnpm-workspace.yaml                           # NEW
├── tsconfig.base.json                            # NEW
├── apps/
│   ├── gateway/                                  # NEW
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── vitest.config.ts
│   │   ├── .env.example
│   │   ├── src/
│   │   │   ├── index.ts                          # Express bootstrap
│   │   │   ├── config.ts                         # env loader
│   │   │   ├── db/
│   │   │   │   ├── supabase.ts                   # client
│   │   │   │   └── cache.ts                      # container_mapping cache
│   │   │   ├── api/
│   │   │   │   ├── healthz.ts
│   │   │   │   ├── purchase.ts
│   │   │   │   └── status.ts
│   │   │   └── provisioning/
│   │   │       ├── azure.ts                      # SDK wrappers
│   │   │       ├── manager.ts                    # orchestrator
│   │   │       └── recovery.ts                   # startup recovery
│   │   └── test/
│   │       ├── db/cache.test.ts
│   │       ├── api/purchase.test.ts
│   │       ├── api/status.test.ts
│   │       ├── provisioning/manager.test.ts
│   │       └── provisioning/recovery.test.ts
│   └── web/                                      # NEW (空占位)
│       └── package.json
├── packages/
│   └── shared/                                   # NEW
│       ├── package.json
│       ├── tsconfig.json
│       └── src/
│           ├── index.ts
│           ├── types.ts                          # DB row types
│           └── contracts.ts                      # API + SSE types
├── infra/
│   ├── bicep/                                    # NEW
│   │   ├── main.bicep
│   │   └── parameters.dev.json
│   ├── supabase/                                 # NEW
│   │   └── migrations/
│   │       └── 0001_init.sql
│   └── README.md                                 # NEW
└── docs/                                          # 已存在
    └── superpowers/...
```

---

## Dependency Graph

```
[Task 1: monorepo root]
       │
       ├─> [Task 2: packages/shared]
       │           │
       │           ▼
       │   [Task 3: apps/gateway skeleton]   [Task 4: Supabase + migrations]
       │           │                                   │
       │           └─────────────┬─────────────────────┘
       │                         ▼
       │           [Task 5: Supabase client wrapper]
       │                         ▼
       │           [Task 6: container_mapping cache]
       │                         │
       │                         ├─> [Task 9: GET /api/status]
       │                         ▼
       │           [Task 7: Azure SDK wrappers]
       │                         ▼
       │           [Task 8: provisioning manager]
       │                         ▼
       │                  [Task 10: POST /api/purchase]
       │                         ▼
       │                  [Task 11: provisioning recovery]
       │                         ▼
       └─> [Task 12: Bicep templates] ─> [Task 13: 真实 Azure 资源初始化]
                                                ▼
                                  [Task 14: 端到端集成测试]
```

---

## Task 1: 初始化 monorepo 根

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `.gitignore`
- Create: `.npmrc`

非 TDD 任务，纯配置初始化。

- [ ] **Step 1.1: 初始化 git 仓库**

```bash
cd /Users/yanjiayi/workspace/laifu
git init
git branch -M main
```

- [ ] **Step 1.2: 创建 `package.json`（根）**

```json
{
  "name": "lingxi",
  "version": "0.0.1",
  "private": true,
  "description": "灵犀 — 数字员工平台 monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Step 1.3: 创建 `pnpm-workspace.yaml`**

```yaml
packages:
  - apps/*
  - packages/*
```

- [ ] **Step 1.4: 创建 `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "sourceMap": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true
  }
}
```

- [ ] **Step 1.5: 创建 `.gitignore`**

```
node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.vitest/
coverage/
```

- [ ] **Step 1.6: 创建 `.npmrc`**

```
strict-peer-dependencies=false
shamefully-hoist=false
```

- [ ] **Step 1.7: 跑一次 `pnpm install` 验证 workspace 配置 OK**

```bash
pnpm install
```

预期：`No projects found` 或安装了 TypeScript devDependencies。无报错即可。

- [ ] **Step 1.8: Commit**

```bash
git add .
git commit -m "chore: init monorepo with pnpm workspaces"
```

---

## Task 2: 创建 `packages/shared`（类型契约单一来源）

**Files:**
- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/index.ts`
- Create: `packages/shared/src/types.ts`
- Create: `packages/shared/src/contracts.ts`

依据 spec §3 (Container HTTP) + §6 (DB schema)。

- [ ] **Step 2.1: 创建目录**

```bash
mkdir -p packages/shared/src
```

- [ ] **Step 2.2: 创建 `packages/shared/package.json`**

```json
{
  "name": "@lingxi/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  }
}
```

- [ ] **Step 2.3: 创建 `packages/shared/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 2.4: 创建 `packages/shared/src/types.ts`**

依据 spec §6 各表 DDL 落 TS 类型。

```typescript
// DB row types — 与 infra/supabase/migrations/0001_init.sql 中表结构一一对应

export type ContainerStatus = 'provisioning' | 'ready' | 'failed';
export type MessageSource = 'web' | 'wechat';
export type WechatSessionStatus = 'active' | 'expired' | 'disabled';

export interface User {
  id: string;                  // uuid
  wx_unionid: string;
  nickname: string | null;
  avatar_url: string | null;
  created_at: string;
}

export interface ContainerMapping {
  user_id: string;
  container_name: string;
  container_url: string | null;
  status: ContainerStatus;
  provisioning_step: string | null;
  progress_pct: number;
  error_message: string | null;
  azure_files_share: string | null;
  created_at: string;
  ready_at: string | null;
}

export interface WechatSession {
  user_id: string;
  bot_token: string;
  expires_at: string;
  status: WechatSessionStatus;
  bound_wx_nick: string | null;
  updated_at: string;
}

export interface ContextToken {
  user_id: string;
  contact_id: string;
  token: string;
  updated_at: string;
}

export interface Thread {
  id: string;
  user_id: string;
  source: MessageSource;
  title: string | null;
  created_at: string;
  updated_at: string;
  archived: boolean;
}
```

- [ ] **Step 2.5: 创建 `packages/shared/src/contracts.ts`**

依据 spec §3。

```typescript
// === Container HTTP 契约 (Gateway → Container) ===

export interface ChatStartRequest {
  session_id: string;          // e.g. "web:thr_abc123" or "wechat:main"
  message: string;
  source: 'web' | 'wechat';
}

export interface ChatStartResponse {
  stream_id: string;
}

// SSE event payloads
export interface SSEEventToken {
  text: string;
}

export interface SSEEventTool {
  name: string;
  preview: string;
}

export interface SSEEventDone {
  full_reply: string;
  session_id: string;
}

export interface SSEEventError {
  message: string;
  trace?: string;
}

// === Gateway Web API 契约 (Web → Gateway) ===

export interface PurchaseRequest {
  // 当前无 body 字段；MVP 全免费、单套餐
}

export interface PurchaseResponse {
  user_id: string;
  status: 'provisioning' | 'ready' | 'failed';
}

export interface StatusResponse {
  status: 'provisioning' | 'ready' | 'failed';
  provisioning_step: string | null;
  progress_pct: number;
  error_message: string | null;
}
```

- [ ] **Step 2.6: 创建 `packages/shared/src/index.ts`**

```typescript
export * from './types.js';
export * from './contracts.js';
```

- [ ] **Step 2.7: 验证类型编译通过**

```bash
pnpm --filter @lingxi/shared lint
```

预期：无输出（成功 type-check）。

- [ ] **Step 2.8: Commit**

```bash
git add packages/
git commit -m "feat(shared): add DB row types and API contracts"
```

---

## Task 3: 创建 `apps/gateway` 骨架 + `/healthz`

**Files:**
- Create: `apps/gateway/package.json`
- Create: `apps/gateway/tsconfig.json`
- Create: `apps/gateway/vitest.config.ts`
- Create: `apps/gateway/.env.example`
- Create: `apps/gateway/src/index.ts`
- Create: `apps/gateway/src/config.ts`
- Create: `apps/gateway/src/api/healthz.ts`
- Create: `apps/gateway/test/api/healthz.test.ts`

- [ ] **Step 3.1: 创建目录**

```bash
mkdir -p apps/gateway/src/{api,db,provisioning}
mkdir -p apps/gateway/test/{api,db,provisioning}
mkdir -p apps/web
echo '{"name":"@lingxi/web","private":true,"version":"0.0.1"}' > apps/web/package.json
```

`apps/web` 当前只做空占位，避免 pnpm-workspace.yaml 报错；Phase 1.3 再填实。

- [ ] **Step 3.2: 创建 `apps/gateway/package.json`**

```json
{
  "name": "@lingxi/gateway",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@lingxi/shared": "workspace:*",
    "@supabase/supabase-js": "^2.39.0",
    "@azure/arm-appcontainers": "^2.0.0",
    "@azure/arm-storage": "^18.2.0",
    "@azure/identity": "^4.0.0",
    "express": "^4.19.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "supertest": "^6.3.4",
    "@types/supertest": "^6.0.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
```

- [ ] **Step 3.3: 创建 `apps/gateway/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node"]
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3.4: 创建 `apps/gateway/vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: false,
    environment: 'node',
    include: ['test/**/*.test.ts'],
  },
});
```

- [ ] **Step 3.5: 创建 `apps/gateway/.env.example`**

```
PORT=3000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Azure
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=rg-lingxi-dev
AZURE_LOCATION=eastasia
AZURE_CONTAINER_APPS_ENV=cae-lingxi-dev
AZURE_STORAGE_ACCOUNT=stlingxidev
AZURE_ACR_LOGIN_SERVER=acrlingxidev.azurecr.io
HERMES_IMAGE_TAG=hermes-base:v1
```

- [ ] **Step 3.6: 安装依赖**

```bash
pnpm install
```

预期：依赖装好，无 unresolved peer。

- [ ] **Step 3.7: 写 failing test for `/healthz`**

`apps/gateway/test/api/healthz.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import request from 'supertest';
import { createApp } from '../../src/index.js';

describe('GET /healthz', () => {
  it('returns 200 with ok=true and uptime_seconds', async () => {
    const app = createApp();
    const res = await request(app).get('/healthz');
    expect(res.status).toBe(200);
    expect(res.body.ok).toBe(true);
    expect(typeof res.body.uptime_seconds).toBe('number');
  });
});
```

- [ ] **Step 3.8: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：`Cannot find module '../../src/index.js'` 或类似的 module-not-found 错。

- [ ] **Step 3.9: 实现 `apps/gateway/src/config.ts`**

```typescript
const required = (key: string): string => {
  const v = process.env[key];
  if (!v) throw new Error(`Missing env var: ${key}`);
  return v;
};

export const config = {
  port: parseInt(process.env.PORT ?? '3000', 10),
  supabase: {
    url: process.env.SUPABASE_URL ?? '',
    serviceRoleKey: process.env.SUPABASE_SERVICE_ROLE_KEY ?? '',
  },
  azure: {
    subscriptionId: process.env.AZURE_SUBSCRIPTION_ID ?? '',
    resourceGroup: process.env.AZURE_RESOURCE_GROUP ?? '',
    location: process.env.AZURE_LOCATION ?? 'eastasia',
    containerAppsEnv: process.env.AZURE_CONTAINER_APPS_ENV ?? '',
    storageAccount: process.env.AZURE_STORAGE_ACCOUNT ?? '',
    acrLoginServer: process.env.AZURE_ACR_LOGIN_SERVER ?? '',
    hermesImageTag: process.env.HERMES_IMAGE_TAG ?? 'hermes-base:v1',
  },
};

// 仅在实际启动 server 时校验，单元测试可跳过
export const validateConfig = () => {
  required('SUPABASE_URL');
  required('SUPABASE_SERVICE_ROLE_KEY');
  required('AZURE_SUBSCRIPTION_ID');
  required('AZURE_RESOURCE_GROUP');
};
```

- [ ] **Step 3.10: 实现 `apps/gateway/src/api/healthz.ts`**

```typescript
import { Router, type Request, type Response } from 'express';

const startedAt = Date.now();

export const healthzRouter = Router();

healthzRouter.get('/healthz', (_req: Request, res: Response) => {
  res.json({
    ok: true,
    uptime_seconds: Math.floor((Date.now() - startedAt) / 1000),
  });
});
```

- [ ] **Step 3.11: 实现 `apps/gateway/src/index.ts`**

```typescript
import express, { type Express } from 'express';
import { healthzRouter } from './api/healthz.js';
import { config, validateConfig } from './config.js';

export const createApp = (): Express => {
  const app = express();
  app.use(express.json());
  app.use(healthzRouter);
  return app;
};

// 入口（仅在直接运行时启动，单元测试 import 不会触发）
if (import.meta.url === `file://${process.argv[1]}`) {
  validateConfig();
  const app = createApp();
  app.listen(config.port, () => {
    console.log(`[gateway] listening on :${config.port}`);
  });
}
```

- [ ] **Step 3.12: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：1 test passed。

- [ ] **Step 3.13: Commit**

```bash
git add apps/
git commit -m "feat(gateway): skeleton with Express + /healthz"
```

---

## Task 4: Supabase 项目 + 表迁移

**Files:**
- Create: `infra/supabase/migrations/0001_init.sql`
- Create: `infra/README.md`

依据 spec §6 各表 DDL。

- [ ] **Step 4.1: 在 Supabase Dashboard 创建项目**

手动操作：
1. 登 https://supabase.com/dashboard
2. New project: name `lingxi-dev`, region `West US`（free tier）
3. 设 db password 并保存

- [ ] **Step 4.2: 拿到项目 URL 和 service_role key**

Dashboard → Project Settings → API：
- 记 `Project URL`
- 记 `service_role secret`（**绝不能 commit**，放 `.env.local`）

- [ ] **Step 4.3: 在 gateway 创建 `.env.local`（不 commit）**

```bash
cp apps/gateway/.env.example apps/gateway/.env.local
# 编辑，填入真实 SUPABASE_URL 和 SUPABASE_SERVICE_ROLE_KEY
```

确认 `.env.local` 已被 root `.gitignore` 排除。

- [ ] **Step 4.4: 创建迁移目录和 `infra/supabase/migrations/0001_init.sql`**

```bash
mkdir -p infra/supabase/migrations
```

`infra/supabase/migrations/0001_init.sql`：

```sql
-- 灵犀 MVP 初始 schema
-- spec: docs/superpowers/specs/2026-05-30-lingxi-mvp-spec.md §6

create extension if not exists "pgcrypto";

-- §6.1 users
create table users (
  id         uuid primary key default gen_random_uuid(),
  wx_unionid text unique not null,
  nickname   text,
  avatar_url text,
  created_at timestamptz default now()
);

-- §6.2 container_mapping
create table container_mapping (
  user_id            uuid primary key references users(id) on delete cascade,
  container_name     text not null unique,
  container_url      text,
  status             text not null check (status in ('provisioning','ready','failed')),
  provisioning_step  text,
  progress_pct       int default 0,
  error_message      text,
  azure_files_share  text,
  created_at         timestamptz default now(),
  ready_at           timestamptz
);

-- §6.3 wechat_sessions
create table wechat_sessions (
  user_id        uuid primary key references users(id) on delete cascade,
  bot_token      text not null,
  expires_at     timestamptz not null,
  status         text not null default 'active' check (status in ('active','expired','disabled')),
  bound_wx_nick  text,
  updated_at     timestamptz default now()
);

-- §6.4 context_tokens
create table context_tokens (
  user_id    uuid not null references users(id) on delete cascade,
  contact_id text not null,
  token      text not null,
  updated_at timestamptz default now(),
  primary key (user_id, contact_id)
);

-- §6.5 threads
create table threads (
  id         text primary key,
  user_id    uuid not null references users(id) on delete cascade,
  source     text not null check (source in ('web','wechat')),
  title      text,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  archived   boolean default false
);

create index threads_user_updated on threads (user_id, updated_at desc);
```

- [ ] **Step 4.5: 创建 `infra/README.md`**

```markdown
# 灵犀 — Infra

## Supabase
迁移文件在 `supabase/migrations/`。
应用方式（dev 阶段）：在 Supabase Dashboard → SQL Editor 复制粘贴运行 `0001_init.sql`。
Phase 2 之后会切到 supabase CLI 自动化：`supabase db push`。

## Azure (Bicep)
Bicep 模板在 `bicep/`。详见 Task 12 + 13。
```

- [ ] **Step 4.6: 在 Supabase Dashboard SQL Editor 执行 0001_init.sql**

手动复制 `0001_init.sql` 内容粘贴运行。预期：5 张表创建成功。

- [ ] **Step 4.7: 验证表存在**

Dashboard → Table Editor，左侧应看到 5 张表。或用 SQL Editor 执行：

```sql
select table_name from information_schema.tables where table_schema = 'public';
```

预期看到：`users`, `container_mapping`, `wechat_sessions`, `context_tokens`, `threads`。

- [ ] **Step 4.8: Commit（不含 .env.local）**

```bash
git add infra/
git commit -m "infra(supabase): add 0001_init migration with 5 tables"
```

---

## Task 5: Supabase client wrapper

**Files:**
- Create: `apps/gateway/src/db/supabase.ts`

非 TDD（thin wrapper，没有业务逻辑）。

- [ ] **Step 5.1: 实现 `apps/gateway/src/db/supabase.ts`**

```typescript
import { createClient, type SupabaseClient } from '@supabase/supabase-js';
import { config } from '../config.js';

let _client: SupabaseClient | null = null;

export const getSupabase = (): SupabaseClient => {
  if (!_client) {
    if (!config.supabase.url || !config.supabase.serviceRoleKey) {
      throw new Error('Supabase config missing (SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY)');
    }
    _client = createClient(config.supabase.url, config.supabase.serviceRoleKey, {
      auth: { persistSession: false },
    });
  }
  return _client;
};

// 单元测试用
export const _resetSupabase = () => {
  _client = null;
};
```

- [ ] **Step 5.2: 用 dev 环境跑一次手动连通性测试**

新建临时文件 `apps/gateway/scripts/probe-supabase.ts`：

```typescript
import 'dotenv/config';
import { getSupabase } from '../src/db/supabase.js';

const main = async () => {
  const sb = getSupabase();
  const { data, error } = await sb.from('users').select('id').limit(1);
  if (error) {
    console.error('FAIL:', error.message);
    process.exit(1);
  }
  console.log('OK, users row count:', data?.length ?? 0);
};

main();
```

跑：
```bash
cd apps/gateway
pnpm exec tsx -r dotenv/config scripts/probe-supabase.ts
```

预期：`OK, users row count: 0`。

- [ ] **Step 5.3: 删 probe 脚本（一次性诊断，不进 repo）**

```bash
rm apps/gateway/scripts/probe-supabase.ts
rmdir apps/gateway/scripts
```

- [ ] **Step 5.4: Commit**

```bash
git add apps/gateway/src/db/supabase.ts
git commit -m "feat(gateway): add Supabase client wrapper"
```

---

## Task 6: `container_mapping` 内存缓存

**Files:**
- Create: `apps/gateway/src/db/cache.ts`
- Create: `apps/gateway/test/db/cache.test.ts`

依据 spec §5.2 「`container_mapping` 状态：DB 是 source of truth，内存只是读缓存」。

- [ ] **Step 6.1: 写 failing test**

`apps/gateway/test/db/cache.test.ts`：

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import type { ContainerMapping } from '@lingxi/shared';
import { ContainerMappingCache } from '../../src/db/cache.js';

const rowReady = (userId: string, url: string): ContainerMapping => ({
  user_id: userId,
  container_name: `hermes-${userId.slice(0, 8)}`,
  container_url: url,
  status: 'ready',
  provisioning_step: null,
  progress_pct: 100,
  error_message: null,
  azure_files_share: `user-${userId.slice(0, 8)}`,
  created_at: new Date().toISOString(),
  ready_at: new Date().toISOString(),
});

describe('ContainerMappingCache', () => {
  let cache: ContainerMappingCache;
  let mockSb: any;

  beforeEach(() => {
    mockSb = {
      from: vi.fn(() => mockSb),
      select: vi.fn(() => mockSb),
      eq: vi.fn(() => mockSb),
      then: undefined,
    };
    cache = new ContainerMappingCache(mockSb);
  });

  it('returns null for unknown user', () => {
    expect(cache.get('unknown-id')).toBeNull();
  });

  it('returns the entry after set()', () => {
    const row = rowReady('u1', 'https://hermes-u1.example.com');
    cache.set(row);
    expect(cache.get('u1')).toEqual(row);
  });

  it('loadAll() populates cache from DB', async () => {
    const rows = [rowReady('u1', 'url1'), rowReady('u2', 'url2')];
    mockSb.select = vi.fn(() => Promise.resolve({ data: rows, error: null }));
    await cache.loadAll();
    expect(cache.get('u1')).toEqual(rows[0]);
    expect(cache.get('u2')).toEqual(rows[1]);
  });

  it('loadAll() throws on DB error', async () => {
    mockSb.select = vi.fn(() => Promise.resolve({ data: null, error: { message: 'boom' } }));
    await expect(cache.loadAll()).rejects.toThrow('boom');
  });
});
```

- [ ] **Step 6.2: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：`Cannot find module '../../src/db/cache.js'` 或类似的找不到模块错误。

- [ ] **Step 6.3: 实现 `apps/gateway/src/db/cache.ts`**

```typescript
import type { SupabaseClient } from '@supabase/supabase-js';
import type { ContainerMapping } from '@lingxi/shared';

export class ContainerMappingCache {
  private readonly map = new Map<string, ContainerMapping>();

  constructor(private readonly sb: SupabaseClient) {}

  get(userId: string): ContainerMapping | null {
    return this.map.get(userId) ?? null;
  }

  set(row: ContainerMapping): void {
    this.map.set(row.user_id, row);
  }

  delete(userId: string): void {
    this.map.delete(userId);
  }

  async loadAll(): Promise<void> {
    const { data, error } = await this.sb.from('container_mapping').select('*');
    if (error) throw new Error(error.message);
    this.map.clear();
    (data as ContainerMapping[]).forEach((row) => this.map.set(row.user_id, row));
  }
}
```

- [ ] **Step 6.4: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：所有 cache 测试 pass。

- [ ] **Step 6.5: Commit**

```bash
git add apps/gateway/src/db/cache.ts apps/gateway/test/db/cache.test.ts
git commit -m "feat(gateway): add container_mapping in-memory cache with DB load"
```

---

## Task 7: Azure SDK wrappers

**Files:**
- Create: `apps/gateway/src/provisioning/azure.ts`

非 TDD（外部 SDK 包装，真值靠集成测试在 Task 14 验证；单元测试 mock 价值有限）。

- [ ] **Step 7.1: 实现 `apps/gateway/src/provisioning/azure.ts`**

```typescript
import { DefaultAzureCredential } from '@azure/identity';
import { ContainerAppsAPIClient } from '@azure/arm-appcontainers';
import { StorageManagementClient } from '@azure/arm-storage';
import { config } from '../config.js';

const credential = new DefaultAzureCredential();

const _containerApps: ContainerAppsAPIClient | null = null;
const _storage: StorageManagementClient | null = null;

const getContainerApps = (): ContainerAppsAPIClient => {
  return (
    _containerApps ?? new ContainerAppsAPIClient(credential, config.azure.subscriptionId)
  );
};

const getStorage = (): StorageManagementClient => {
  return _storage ?? new StorageManagementClient(credential, config.azure.subscriptionId);
};

/**
 * 在共享 Storage Account 下为该用户创建一个独立的 Azure Files share。
 * MVP 阶段所有用户的 share 都在一个 Storage Account 下（≤100 share 上限够用）。
 */
export const createFileShare = async (shareName: string): Promise<void> => {
  const client = getStorage();
  await client.fileShares.create(
    config.azure.resourceGroup,
    config.azure.storageAccount,
    shareName,
    {},
  );
};

/**
 * 创建 Container App 并轮询直到 ready。
 * 返回 FQDN。
 */
export const createContainerApp = async (params: {
  containerName: string;
  shareName: string;
}): Promise<string> => {
  const client = getContainerApps();
  const envFqdn = `/subscriptions/${config.azure.subscriptionId}/resourceGroups/${config.azure.resourceGroup}/providers/Microsoft.App/managedEnvironments/${config.azure.containerAppsEnv}`;
  const poller = await client.containerApps.beginCreateOrUpdate(
    config.azure.resourceGroup,
    params.containerName,
    {
      location: config.azure.location,
      managedEnvironmentId: envFqdn,
      configuration: {
        ingress: {
          external: true,                // Phase 1.2 用外部 ingress；1.5 改 internal
          targetPort: 8080,
          allowInsecure: false,
        },
      },
      template: {
        containers: [
          {
            name: 'hermes',
            image: `${config.azure.acrLoginServer}/${config.azure.hermesImageTag}`,
            resources: { cpu: 1, memory: '2Gi' },
            volumeMounts: [{ volumeName: 'home', mountPath: '/home/hermes' }],
          },
        ],
        volumes: [
          {
            name: 'home',
            storageType: 'AzureFile',
            storageName: params.shareName,
          },
        ],
        scale: { minReplicas: 0, maxReplicas: 1 },
      },
    },
  );
  const result = await poller.pollUntilDone({ intervalInMs: 5000 });
  if (!result.configuration?.ingress?.fqdn) {
    throw new Error('ContainerApp created but no fqdn returned');
  }
  return `https://${result.configuration.ingress.fqdn}`;
};

/**
 * 查 Container App 的当前 provisioningState（用于 startup 恢复时续追）。
 */
export const getContainerAppState = async (
  containerName: string,
): Promise<{ state: string | undefined; fqdn: string | null }> => {
  const client = getContainerApps();
  const r = await client.containerApps.get(config.azure.resourceGroup, containerName);
  return {
    state: r.provisioningState,
    fqdn: r.configuration?.ingress?.fqdn ? `https://${r.configuration.ingress.fqdn}` : null,
  };
};
```

> **注意**：`createContainerApp` 返回时 Container App 已被 Azure 标记 `Succeeded` 但内部 image 可能仍在 pull——这是已知的 ACA 行为。生产场景应该再调用 `readinessProbe` 端点（`/healthz`）确认。MVP 阶段先信 Azure 的 Succeeded。

- [ ] **Step 7.2: type-check 通过**

```bash
pnpm --filter @lingxi/gateway lint
```

预期：无错。

- [ ] **Step 7.3: Commit**

```bash
git add apps/gateway/src/provisioning/azure.ts
git commit -m "feat(gateway): add Azure SDK wrappers for Files + Container Apps"
```

---

## Task 8: Provisioning manager（核心异步流程）

**Files:**
- Create: `apps/gateway/src/provisioning/manager.ts`
- Create: `apps/gateway/test/provisioning/manager.test.ts`

依据 spec §7.1 状态机。每步必须更新 `container_mapping.provisioning_step` + `progress_pct`，对应 §1.1 用户旅程里 6 步进度文案。

- [ ] **Step 8.1: 写 failing test**

`apps/gateway/test/provisioning/manager.test.ts`：

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { provisionContainer } from '../../src/provisioning/manager.js';

const finalReadyRow = {
  user_id: 'u1',
  container_name: 'hermes-u1abc',
  azure_files_share: 'user-u1abc',
  status: 'ready' as const,
  container_url: 'https://hermes-u1abc.example.com',
  provisioning_step: '灵犀助理上岗完成',
  progress_pct: 100,
  error_message: null,
  created_at: new Date().toISOString(),
  ready_at: new Date().toISOString(),
};

describe('provisionContainer', () => {
  let mockSb: any;
  let mockCache: any;
  let mockAzure: any;
  const updates: any[] = [];
  let thenResult: any;

  beforeEach(() => {
    updates.length = 0;
    thenResult = { data: null, error: null };
    // 关键模式：mockSb 自身是 thenable（让 update().eq() 可 await），
    // 同时所有链式方法返回 mockSb，single() 返回 Promise（带最终数据）。
    mockSb = {
      from: vi.fn(() => mockSb),
      update: vi.fn((u: any) => { updates.push(u); return mockSb; }),
      insert: vi.fn(() => mockSb),
      select: vi.fn(() => mockSb),
      eq: vi.fn(() => mockSb),
      single: vi.fn(() => Promise.resolve({ data: finalReadyRow, error: null })),
      then: (resolve: any) => resolve(thenResult),  // await mockSb 时触发
    };
    mockCache = { set: vi.fn(), delete: vi.fn() };
    mockAzure = {
      createFileShare: vi.fn(() => Promise.resolve()),
      createContainerApp: vi.fn(() => Promise.resolve('https://hermes-u1abc.example.com')),
    };
  });

  it('updates provisioning_step through all phases and marks ready on success', async () => {
    await provisionContainer({
      userId: 'u1',
      containerName: 'hermes-u1abc',
      shareName: 'user-u1abc',
      sb: mockSb,
      cache: mockCache,
      azure: mockAzure,
    });

    // 至少有 6 次 step 更新 + 1 次 ready 更新
    const stepUpdates = updates.filter((u) => 'provisioning_step' in u);
    expect(stepUpdates.length).toBeGreaterThanOrEqual(6);

    // 最后一次更新写 status=ready 和 url
    const lastReady = updates.find((u) => u.status === 'ready');
    expect(lastReady).toBeDefined();
    expect(lastReady.container_url).toBe('https://hermes-u1abc.example.com');
    expect(lastReady.progress_pct).toBe(100);

    // Azure SDK 调用顺序
    expect(mockAzure.createFileShare).toHaveBeenCalledWith('user-u1abc');
    expect(mockAzure.createContainerApp).toHaveBeenCalledWith({
      containerName: 'hermes-u1abc',
      shareName: 'user-u1abc',
    });

    // 缓存被刷新
    expect(mockCache.set).toHaveBeenCalled();
  });

  it('marks failed if Azure throws', async () => {
    mockAzure.createContainerApp = vi.fn(() =>
      Promise.reject(new Error('Azure quota exceeded')),
    );

    await provisionContainer({
      userId: 'u1',
      containerName: 'hermes-u1abc',
      shareName: 'user-u1abc',
      sb: mockSb,
      cache: mockCache,
      azure: mockAzure,
    });

    const failedUpdate = updates.find((u) => u.status === 'failed');
    expect(failedUpdate).toBeDefined();
    expect(failedUpdate.error_message).toContain('Azure quota exceeded');
  });
});
```

- [ ] **Step 8.2: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：`Cannot find module '../../src/provisioning/manager.js'`。

- [ ] **Step 8.3: 实现 `apps/gateway/src/provisioning/manager.ts`**

```typescript
import type { SupabaseClient } from '@supabase/supabase-js';
import type { ContainerMappingCache } from '../db/cache.js';
import type { ContainerMapping } from '@lingxi/shared';

// 与 spec §1.1 用户旅程里 6 步进度文案一致
const STEPS = [
  { step: '正在创建账户与订单', pct: 5 },
  { step: '正在生成数字助理实例', pct: 20 },
  { step: '为助理分配 DID 与 Agent 运行时', pct: 40 },
  { step: '初始化默认能力', pct: 70 },
  { step: '装载基础知识库', pct: 90 },
  { step: '灵犀助理上岗完成', pct: 100 },
] as const;

export interface AzureProvisioner {
  createFileShare(shareName: string): Promise<void>;
  createContainerApp(params: { containerName: string; shareName: string }): Promise<string>;
}

export interface ProvisionContainerArgs {
  userId: string;
  containerName: string;
  shareName: string;
  sb: SupabaseClient;
  cache: ContainerMappingCache;
  azure: AzureProvisioner;
}

const updateStep = async (
  sb: SupabaseClient,
  userId: string,
  step: string,
  pct: number,
): Promise<void> => {
  await sb.from('container_mapping').update({ provisioning_step: step, progress_pct: pct }).eq('user_id', userId);
};

export const provisionContainer = async (args: ProvisionContainerArgs): Promise<void> => {
  const { userId, containerName, shareName, sb, cache, azure } = args;

  try {
    // Step 0: 已经在 INSERT 时初始化为 provisioning，跳过

    await updateStep(sb, userId, STEPS[0].step, STEPS[0].pct);

    await updateStep(sb, userId, STEPS[1].step, STEPS[1].pct);
    await azure.createFileShare(shareName);

    await updateStep(sb, userId, STEPS[2].step, STEPS[2].pct);
    const url = await azure.createContainerApp({ containerName, shareName });

    await updateStep(sb, userId, STEPS[3].step, STEPS[3].pct);
    // MVP: 默认能力靠 Container 启动时自带，Gateway 无需额外触发

    await updateStep(sb, userId, STEPS[4].step, STEPS[4].pct);
    // MVP: 知识库装载靠 Container 启动时自带

    // 最终 ready
    const readyAt = new Date().toISOString();
    await sb
      .from('container_mapping')
      .update({
        status: 'ready',
        container_url: url,
        provisioning_step: STEPS[5].step,
        progress_pct: STEPS[5].pct,
        ready_at: readyAt,
      })
      .eq('user_id', userId);

    // 刷缓存
    const { data } = await sb.from('container_mapping').select('*').eq('user_id', userId).single();
    if (data) cache.set(data as ContainerMapping);
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    await sb
      .from('container_mapping')
      .update({ status: 'failed', error_message: msg })
      .eq('user_id', userId);
    cache.delete(userId);
    console.error(`[provisioning] failed for user ${userId}:`, msg);
  }
};
```

- [ ] **Step 8.4: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：所有 manager 测试 pass。

- [ ] **Step 8.5: Commit**

```bash
git add apps/gateway/src/provisioning/manager.ts apps/gateway/test/provisioning/manager.test.ts
git commit -m "feat(gateway): add provisioning manager with 6-step progress"
```

---

## Task 9: `GET /api/status`

**Files:**
- Create: `apps/gateway/src/api/status.ts`
- Create: `apps/gateway/test/api/status.test.ts`
- Modify: `apps/gateway/src/index.ts`

依据 spec §9.3：前端轮询此接口拿 `progress_pct` + `provisioning_step` 显示 6 步进度。

- [ ] **Step 9.1: 写 failing test**

`apps/gateway/test/api/status.test.ts`：

```typescript
import { describe, it, expect, vi } from 'vitest';
import request from 'supertest';
import { createApp } from '../../src/index.js';
import { ContainerMappingCache } from '../../src/db/cache.js';

const setupCache = () => {
  const cache = new ContainerMappingCache({} as any);
  cache.set({
    user_id: 'u1',
    container_name: 'hermes-u1abc',
    container_url: null,
    status: 'provisioning',
    provisioning_step: '正在生成数字助理实例',
    progress_pct: 20,
    error_message: null,
    azure_files_share: 'user-u1abc',
    created_at: new Date().toISOString(),
    ready_at: null,
  });
  return cache;
};

describe('GET /api/status', () => {
  it('returns 404 when user has no container_mapping row', async () => {
    const app = createApp({ cache: new ContainerMappingCache({} as any) });
    const res = await request(app).get('/api/status').set('x-user-id', 'unknown');
    expect(res.status).toBe(404);
  });

  it('returns status fields when row exists', async () => {
    const app = createApp({ cache: setupCache() });
    const res = await request(app).get('/api/status').set('x-user-id', 'u1');
    expect(res.status).toBe(200);
    expect(res.body).toEqual({
      status: 'provisioning',
      provisioning_step: '正在生成数字助理实例',
      progress_pct: 20,
      error_message: null,
    });
  });
});
```

> 说明：MVP Phase 1.2 用 `x-user-id` header 携带 user_id（信任，无 auth）。Phase 1.3 加微信 OAuth 后改用 session cookie。

- [ ] **Step 9.2: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：`createApp` 不接受 options + `Cannot find module '../../src/api/status.js'`。

- [ ] **Step 9.3: 实现 `apps/gateway/src/api/status.ts`**

```typescript
import { Router, type Request, type Response } from 'express';
import type { ContainerMappingCache } from '../db/cache.js';
import type { StatusResponse } from '@lingxi/shared';

export const buildStatusRouter = (cache: ContainerMappingCache): Router => {
  const router = Router();

  router.get('/api/status', (req: Request, res: Response) => {
    const userId = req.header('x-user-id');
    if (!userId) return res.status(400).json({ error: 'x-user-id required' });

    const row = cache.get(userId);
    if (!row) return res.status(404).json({ error: 'no container mapping' });

    const body: StatusResponse = {
      status: row.status,
      provisioning_step: row.provisioning_step,
      progress_pct: row.progress_pct,
      error_message: row.error_message,
    };
    res.json(body);
  });

  return router;
};
```

- [ ] **Step 9.4: 改 `apps/gateway/src/index.ts` 让 `createApp` 接受 options**

```typescript
import express, { type Express } from 'express';
import { healthzRouter } from './api/healthz.js';
import { buildStatusRouter } from './api/status.js';
import { ContainerMappingCache } from './db/cache.js';
import { config, validateConfig } from './config.js';
import { getSupabase } from './db/supabase.js';

export interface CreateAppOptions {
  cache?: ContainerMappingCache;
}

export const createApp = (opts: CreateAppOptions = {}): Express => {
  const app = express();
  app.use(express.json());

  // Unit test 可以传入预设的 cache；正式跑用真实 Supabase
  const cache = opts.cache ?? new ContainerMappingCache(getSupabase());

  app.use(healthzRouter);
  app.use(buildStatusRouter(cache));

  return app;
};

if (import.meta.url === `file://${process.argv[1]}`) {
  validateConfig();
  const sb = getSupabase();
  const cache = new ContainerMappingCache(sb);
  cache.loadAll().then(() => {
    const app = createApp({ cache });
    app.listen(config.port, () => {
      console.log(`[gateway] listening on :${config.port}`);
    });
  }).catch((err) => {
    console.error('[gateway] failed to load cache:', err);
    process.exit(1);
  });
}
```

- [ ] **Step 9.5: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：所有测试 pass。

- [ ] **Step 9.6: Commit**

```bash
git add apps/gateway/src/api/status.ts apps/gateway/src/index.ts apps/gateway/test/api/status.test.ts
git commit -m "feat(gateway): add GET /api/status with cache lookup"
```

---

## Task 10: `POST /api/purchase`

**Files:**
- Create: `apps/gateway/src/api/purchase.ts`
- Create: `apps/gateway/test/api/purchase.test.ts`
- Modify: `apps/gateway/src/index.ts`

依据 spec §7.1 + §8.2。**关键**：handler 立刻返回 200 + status=provisioning；异步任务 fire-and-forget。

- [ ] **Step 10.1: 写 failing test**

`apps/gateway/test/api/purchase.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import request from 'supertest';
import { createApp } from '../../src/index.js';
import { ContainerMappingCache } from '../../src/db/cache.js';

describe('POST /api/purchase', () => {
  let mockSb: any;
  let cache: ContainerMappingCache;
  let inserted: any;
  let thenResult: any;

  beforeEach(() => {
    inserted = null;
    thenResult = { data: null, error: null };
    mockSb = {
      from: vi.fn(() => mockSb),
      insert: vi.fn((row: any) => { inserted = row; return mockSb; }),
      select: vi.fn(() => mockSb),
      eq: vi.fn(() => mockSb),
      single: vi.fn(() => Promise.resolve({ data: inserted, error: null })),
      then: (resolve: any) => resolve(thenResult),
    };
    cache = new ContainerMappingCache(mockSb);
  });

  it('inserts container_mapping row, returns provisioning, kicks off async task', async () => {
    const provisioner = vi.fn(() => Promise.resolve());
    const app = createApp({ cache, sb: mockSb, provisioner });

    const res = await request(app).post('/api/purchase').set('x-user-id', 'u1');

    expect(res.status).toBe(200);
    expect(res.body.status).toBe('provisioning');
    expect(res.body.user_id).toBe('u1');

    expect(inserted).toMatchObject({
      user_id: 'u1',
      status: 'provisioning',
      progress_pct: 0,
    });
    expect(inserted.container_name).toMatch(/^hermes-/);
    expect(inserted.azure_files_share).toMatch(/^user-/);

    // provisioner 被调，但 handler 不等它
    expect(provisioner).toHaveBeenCalledOnce();
  });

  it('400 when x-user-id missing', async () => {
    const app = createApp({ cache, sb: mockSb, provisioner: vi.fn() });
    const res = await request(app).post('/api/purchase');
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 10.2: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：`createApp` 不接受 sb / provisioner + 找不到 `purchase.ts`。

- [ ] **Step 10.3: 实现 `apps/gateway/src/api/purchase.ts`**

```typescript
import { Router, type Request, type Response } from 'express';
import type { SupabaseClient } from '@supabase/supabase-js';
import type { PurchaseResponse } from '@lingxi/shared';
import type { ContainerMappingCache } from '../db/cache.js';

export type ProvisionerFn = (args: { userId: string; containerName: string; shareName: string }) => Promise<void>;

const shortHash = (userId: string): string => userId.replace(/-/g, '').slice(0, 8);

export const buildPurchaseRouter = (
  sb: SupabaseClient,
  cache: ContainerMappingCache,
  provisioner: ProvisionerFn,
): Router => {
  const router = Router();

  router.post('/api/purchase', async (req: Request, res: Response) => {
    const userId = req.header('x-user-id');
    if (!userId) return res.status(400).json({ error: 'x-user-id required' });

    const hash = shortHash(userId);
    const containerName = `hermes-${hash}`;
    const shareName = `user-${hash}`;

    // 幂等：用 ON CONFLICT 防止重复 INSERT
    const { error: insErr } = await sb
      .from('container_mapping')
      .insert({
        user_id: userId,
        container_name: containerName,
        azure_files_share: shareName,
        status: 'provisioning',
        progress_pct: 0,
      });

    if (insErr) {
      // 已经存在 → 直接返回当前状态
      const existing = cache.get(userId);
      if (existing) {
        const body: PurchaseResponse = { user_id: userId, status: existing.status };
        return res.json(body);
      }
      // 真正的写入错误
      return res.status(500).json({ error: insErr.message });
    }

    // 同步更新 cache（先放一个 provisioning 占位）
    const { data } = await sb.from('container_mapping').select('*').eq('user_id', userId).single();
    if (data) cache.set(data as any);

    // 异步触发 provisioning（fire-and-forget）
    provisioner({ userId, containerName, shareName }).catch((err) => {
      console.error(`[purchase] provisioner error for ${userId}:`, err);
    });

    const body: PurchaseResponse = { user_id: userId, status: 'provisioning' };
    res.json(body);
  });

  return router;
};
```

- [ ] **Step 10.4: 改 `apps/gateway/src/index.ts` 让 `createApp` 接受 sb + provisioner**

```typescript
import express, { type Express } from 'express';
import type { SupabaseClient } from '@supabase/supabase-js';
import { healthzRouter } from './api/healthz.js';
import { buildStatusRouter } from './api/status.js';
import { buildPurchaseRouter, type ProvisionerFn } from './api/purchase.js';
import { ContainerMappingCache } from './db/cache.js';
import { config, validateConfig } from './config.js';
import { getSupabase } from './db/supabase.js';
import { provisionContainer } from './provisioning/manager.js';
import * as azure from './provisioning/azure.js';

export interface CreateAppOptions {
  cache?: ContainerMappingCache;
  sb?: SupabaseClient;
  provisioner?: ProvisionerFn;
}

export const createApp = (opts: CreateAppOptions = {}): Express => {
  const app = express();
  app.use(express.json());

  const sb = opts.sb ?? getSupabase();
  const cache = opts.cache ?? new ContainerMappingCache(sb);
  const provisioner: ProvisionerFn = opts.provisioner ?? (async (args) => {
    await provisionContainer({ ...args, sb, cache, azure });
  });

  app.use(healthzRouter);
  app.use(buildStatusRouter(cache));
  app.use(buildPurchaseRouter(sb, cache, provisioner));

  return app;
};

if (import.meta.url === `file://${process.argv[1]}`) {
  validateConfig();
  const sb = getSupabase();
  const cache = new ContainerMappingCache(sb);
  cache.loadAll().then(() => {
    const app = createApp({ cache, sb });
    app.listen(config.port, () => {
      console.log(`[gateway] listening on :${config.port}`);
    });
  }).catch((err) => {
    console.error('[gateway] startup failed:', err);
    process.exit(1);
  });
}
```

- [ ] **Step 10.5: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：所有测试 pass。

- [ ] **Step 10.6: Commit**

```bash
git add apps/gateway/src/api/purchase.ts apps/gateway/src/index.ts apps/gateway/test/api/purchase.test.ts
git commit -m "feat(gateway): add POST /api/purchase with async provisioning"
```

---

## Task 11: 启动恢复（recovery）

**Files:**
- Create: `apps/gateway/src/provisioning/recovery.ts`
- Create: `apps/gateway/test/provisioning/recovery.test.ts`
- Modify: `apps/gateway/src/index.ts`

依据 spec §7.3。

- [ ] **Step 11.1: 写 failing test**

`apps/gateway/test/provisioning/recovery.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { recoverProvisioning } from '../../src/provisioning/recovery.js';

describe('recoverProvisioning', () => {
  let mockSb: any;
  let updates: any[];
  let mockAzure: any;
  let selectResult: any;

  beforeEach(() => {
    updates = [];
    selectResult = { data: [], error: null };
    mockSb = {
      from: vi.fn(() => mockSb),
      select: vi.fn(() => mockSb),
      eq: vi.fn(() => mockSb),
      update: vi.fn((u: any) => { updates.push(u); return mockSb; }),
      then: (resolve: any) => resolve(selectResult),
    };
    mockAzure = { getContainerAppState: vi.fn() };
  });

  it('Succeeded → updates status=ready with fqdn', async () => {
    selectResult = {
      data: [{ user_id: 'u1', container_name: 'hermes-u1' }],
      error: null,
    };
    mockAzure.getContainerAppState.mockResolvedValue({
      state: 'Succeeded',
      fqdn: 'https://hermes-u1.example.com',
    });

    await recoverProvisioning(mockSb, mockAzure);

    expect(updates.some((u) => u.status === 'ready' && u.container_url === 'https://hermes-u1.example.com')).toBe(true);
  });

  it('Failed → updates status=failed', async () => {
    selectResult = {
      data: [{ user_id: 'u1', container_name: 'hermes-u1' }],
      error: null,
    };
    mockAzure.getContainerAppState.mockResolvedValue({ state: 'Failed', fqdn: null });

    await recoverProvisioning(mockSb, mockAzure);

    expect(updates.some((u) => u.status === 'failed')).toBe(true);
  });

  it('not found → updates status=failed with error', async () => {
    selectResult = {
      data: [{ user_id: 'u1', container_name: 'hermes-u1' }],
      error: null,
    };
    mockAzure.getContainerAppState.mockRejectedValue(new Error('ResourceNotFound'));

    await recoverProvisioning(mockSb, mockAzure);

    const failed = updates.find((u) => u.status === 'failed');
    expect(failed).toBeDefined();
    expect(failed.error_message).toContain('ResourceNotFound');
  });
});
```

- [ ] **Step 11.2: 跑测试确认 fail**

```bash
pnpm --filter @lingxi/gateway test
```

预期：找不到 `recovery.js`。

- [ ] **Step 11.3: 实现 `apps/gateway/src/provisioning/recovery.ts`**

```typescript
import type { SupabaseClient } from '@supabase/supabase-js';

export interface AzureStateProbe {
  getContainerAppState(containerName: string): Promise<{ state: string | undefined; fqdn: string | null }>;
}

/**
 * App Service 启动时调用一次：扫表 status=provisioning 的行，
 * 查 Azure 的真实状态，把卡在中间的行推进到 ready/failed。
 */
export const recoverProvisioning = async (
  sb: SupabaseClient,
  azure: AzureStateProbe,
): Promise<void> => {
  const { data, error } = await sb
    .from('container_mapping')
    .select('user_id, container_name')
    .eq('status', 'provisioning');

  if (error) throw new Error(error.message);
  if (!data || data.length === 0) return;

  for (const row of data) {
    try {
      const probe = await azure.getContainerAppState(row.container_name);
      if (probe.state === 'Succeeded' && probe.fqdn) {
        await sb
          .from('container_mapping')
          .update({
            status: 'ready',
            container_url: probe.fqdn,
            ready_at: new Date().toISOString(),
            provisioning_step: '灵犀助理上岗完成',
            progress_pct: 100,
          })
          .eq('user_id', row.user_id);
      } else if (probe.state === 'Failed' || probe.state === 'Canceled') {
        await sb
          .from('container_mapping')
          .update({ status: 'failed', error_message: `Azure state: ${probe.state}` })
          .eq('user_id', row.user_id);
      }
      // else state==='InProgress' / 'Creating' → 暂时不动，下次启动再扫
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      await sb
        .from('container_mapping')
        .update({ status: 'failed', error_message: msg })
        .eq('user_id', row.user_id);
    }
  }
};
```

- [ ] **Step 11.4: 改 `apps/gateway/src/index.ts` 在启动时调 recovery**

`index.ts` 的启动块改为：

```typescript
if (import.meta.url === `file://${process.argv[1]}`) {
  validateConfig();
  const sb = getSupabase();
  const cache = new ContainerMappingCache(sb);

  (async () => {
    console.log('[gateway] recovering stuck provisioning rows...');
    await recoverProvisioning(sb, azure);
    console.log('[gateway] loading cache...');
    await cache.loadAll();
    const app = createApp({ cache, sb });
    app.listen(config.port, () => {
      console.log(`[gateway] listening on :${config.port}`);
    });
  })().catch((err) => {
    console.error('[gateway] startup failed:', err);
    process.exit(1);
  });
}
```

记得 `import { recoverProvisioning } from './provisioning/recovery.js';` 加到顶部。

- [ ] **Step 11.5: 跑测试确认 pass**

```bash
pnpm --filter @lingxi/gateway test
```

预期：全 pass。

- [ ] **Step 11.6: Commit**

```bash
git add apps/gateway/src/provisioning/recovery.ts apps/gateway/src/index.ts apps/gateway/test/provisioning/recovery.test.ts
git commit -m "feat(gateway): add startup recovery for stuck provisioning rows"
```

---

## Task 12: Bicep 模板（Azure 基础设施）

**Files:**
- Create: `infra/bicep/main.bicep`
- Create: `infra/bicep/parameters.dev.json`
- Create: `infra/bicep/deploy.sh`

非 TDD。MVP 用最小够用配置：Resource Group、Container Apps Environment（无 VNet，Phase 1.5 再加）、ACR、Storage Account、App Service B1。

- [ ] **Step 12.1: 创建 `infra/bicep/main.bicep`**

```bicep
@description('Resource name prefix')
param namePrefix string = 'lingxi'

@description('Environment short name')
@allowed(['dev','staging','prod'])
param env string = 'dev'

@description('Azure region')
param location string = 'eastasia'

var rgSuffix = '${namePrefix}-${env}'
var caeName = 'cae-${rgSuffix}'
var acrName = toLower('acr${replace(rgSuffix, '-', '')}')
var storageName = toLower('st${replace(rgSuffix, '-', '')}')
var appServicePlanName = 'asp-${rgSuffix}'
var appServiceName = 'app-${rgSuffix}-gateway'
var laName = 'la-${rgSuffix}'

resource la 'Microsoft.OperationalInsights/workspaces@2022-10-01' = {
  name: laName
  location: location
  properties: { sku: { name: 'PerGB2018' }, retentionInDays: 30 }
}

resource acr 'Microsoft.ContainerRegistry/registries@2023-07-01' = {
  name: acrName
  location: location
  sku: { name: 'Basic' }
  properties: { adminUserEnabled: true }
}

resource storage 'Microsoft.Storage/storageAccounts@2023-05-01' = {
  name: storageName
  location: location
  sku: { name: 'Standard_LRS' }
  kind: 'StorageV2'
  properties: { accessTier: 'Hot', allowBlobPublicAccess: false }
}

resource fileService 'Microsoft.Storage/storageAccounts/fileServices@2023-05-01' = {
  parent: storage
  name: 'default'
}

resource cae 'Microsoft.App/managedEnvironments@2024-03-01' = {
  name: caeName
  location: location
  properties: {
    appLogsConfiguration: {
      destination: 'log-analytics'
      logAnalyticsConfiguration: {
        customerId: la.properties.customerId
        sharedKey: la.listKeys().primarySharedKey
      }
    }
  }
}

resource appServicePlan 'Microsoft.Web/serverfarms@2023-12-01' = {
  name: appServicePlanName
  location: location
  sku: { name: 'B1', tier: 'Basic' }
  kind: 'linux'
  properties: { reserved: true }
}

resource appService 'Microsoft.Web/sites@2023-12-01' = {
  name: appServiceName
  location: location
  properties: {
    serverFarmId: appServicePlan.id
    httpsOnly: true
    siteConfig: {
      linuxFxVersion: 'NODE|22-lts'
      alwaysOn: true
    }
  }
}

output acrLoginServer string = acr.properties.loginServer
output containerAppsEnvId string = cae.id
output storageAccountName string = storage.name
output appServiceHost string = appService.properties.defaultHostName
```

- [ ] **Step 12.2: 创建 `infra/bicep/parameters.dev.json`**

```json
{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "namePrefix": { "value": "lingxi" },
    "env": { "value": "dev" },
    "location": { "value": "eastasia" }
  }
}
```

- [ ] **Step 12.3: 创建 `infra/bicep/deploy.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

ENV=${1:-dev}
RG="rg-lingxi-${ENV}"
LOCATION="eastasia"

# 1. 创建 RG (idempotent)
az group create -n "$RG" -l "$LOCATION" --output none

# 2. 部署 Bicep
az deployment group create \
  -g "$RG" \
  -f "$(dirname "$0")/main.bicep" \
  -p "@$(dirname "$0")/parameters.${ENV}.json" \
  --output table
```

```bash
chmod +x infra/bicep/deploy.sh
```

- [ ] **Step 12.4: Commit**

```bash
git add infra/bicep/
git commit -m "infra(bicep): add MVP Azure resource template (RG/ACR/Storage/CAE/App Service)"
```

---

## Task 13: 部署 Azure 资源 + 填环境变量

非 TDD。手动一次性操作。

- [ ] **Step 13.1: 登 Azure CLI**

```bash
az login
az account show
```

确认 subscription 是你想用的。如不对：`az account set --subscription <id>`。

- [ ] **Step 13.2: 跑 Bicep**

```bash
./infra/bicep/deploy.sh dev
```

预期：deployment 输出表格，看到 acrLoginServer / containerAppsEnvId / storageAccountName / appServiceHost 四个 outputs。**记下这些值**。

- [ ] **Step 13.3: 推一个占位 image 到 ACR（让 Container App 至少能拉到东西）**

```bash
ACR=$(az acr show -n acrlingxidev -g rg-lingxi-dev --query loginServer -o tsv)
az acr login -n acrlingxidev

# 用任意小镜像占位
docker pull mcr.microsoft.com/k8se/quickstart:latest
docker tag mcr.microsoft.com/k8se/quickstart:latest "$ACR/hermes-base:v1"
docker push "$ACR/hermes-base:v1"
```

**注意**：这是临时占位，Phase 1.1 同事完成真实 Hermes Image 后再 retag 替换。

- [ ] **Step 13.4: 把所有真实值填进 `apps/gateway/.env.local`**

```
PORT=3000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...

AZURE_SUBSCRIPTION_ID=<az account show --query id -o tsv>
AZURE_RESOURCE_GROUP=rg-lingxi-dev
AZURE_LOCATION=eastasia
AZURE_CONTAINER_APPS_ENV=cae-lingxi-dev
AZURE_STORAGE_ACCOUNT=stlingxidev
AZURE_ACR_LOGIN_SERVER=acrlingxidev.azurecr.io
HERMES_IMAGE_TAG=hermes-base:v1
```

确认 `.env.local` 在 `.gitignore` 里。

- [ ] **Step 13.5: 给 Container Apps Environment 的 managed identity 授 ACR 拉镜像权限**

```bash
# Azure CLI 创建 Container App 时默认用 system-assigned MI，需要 acrpull 角色
# 由于我们手工创建 ContainerApp，暂时用 admin user pull
az acr update -n acrlingxidev --admin-enabled true
```

> Phase 1.5 改 MI + RBAC，去掉 admin user。

- [ ] **Step 13.6: 跑 Gateway 本地启动一次确认环境通**

```bash
cd apps/gateway
pnpm exec tsx -r dotenv/config src/index.ts
```

预期日志：
```
[gateway] recovering stuck provisioning rows...
[gateway] loading cache...
[gateway] listening on :3000
```

`Ctrl+C` 退出。

- [ ] **Step 13.7: Commit 部署脚本变更（不含 `.env.local`）**

如有：
```bash
git status
# 应只有未跟踪的 .env.local；其余都已 commit
```

---

## Task 14: 端到端集成测试

非 TDD。手动验证 DoD。

- [ ] **Step 14.1: 启 Gateway**

```bash
cd apps/gateway
pnpm exec tsx -r dotenv/config src/index.ts
```

保持运行。

- [ ] **Step 14.2: POST /api/purchase**

新开 terminal：

```bash
curl -X POST http://localhost:3000/api/purchase \
  -H 'x-user-id: 11111111-2222-3333-4444-555555555555'
```

预期返回（**5 秒内**）：
```json
{ "user_id": "11111111-2222-3333-4444-555555555555", "status": "provisioning" }
```

- [ ] **Step 14.3: 重复轮询 /api/status**

```bash
while true; do
  curl -s http://localhost:3000/api/status \
    -H 'x-user-id: 11111111-2222-3333-4444-555555555555'
  echo ""
  sleep 5
done
```

预期：

```json
{"status":"provisioning","provisioning_step":"正在创建账户与订单","progress_pct":5,"error_message":null}
{"status":"provisioning","provisioning_step":"正在生成数字助理实例","progress_pct":20,"error_message":null}
{"status":"provisioning","provisioning_step":"为助理分配 DID 与 Agent 运行时","progress_pct":40,"error_message":null}
...
{"status":"ready","provisioning_step":"灵犀助理上岗完成","progress_pct":100,"error_message":null}
```

`Ctrl+C` 停轮询。

- [ ] **Step 14.4: Azure Portal 验证**

打开 https://portal.azure.com → Resource Group `rg-lingxi-dev`：
- ✅ 应看到一个新的 Container App，名字形如 `hermes-11111111`
- ✅ 应看到一个新的 File share，名字形如 `user-11111111`

- [ ] **Step 14.5: Supabase Dashboard 验证**

Dashboard → Table Editor → `container_mapping`：
- ✅ 应有一行，user_id 匹配，status='ready'，container_url 非空

- [ ] **Step 14.6: 测启动恢复**

`Ctrl+C` 停 Gateway。在 Supabase Dashboard SQL Editor 跑：

```sql
update container_mapping set status='provisioning' where user_id='11111111-2222-3333-4444-555555555555';
```

重启 Gateway。预期日志显示 `recovering stuck provisioning rows...`，几秒后 DB 中该行 status 又被改回 `ready`（因为 Azure 那边其实已经 Succeeded）。

- [ ] **Step 14.7: 测失败路径**

清理：

```bash
# 用 Azure CLI 删 Container App
az containerapp delete -n hermes-11111111 -g rg-lingxi-dev --yes
```

在 Supabase SQL Editor：

```sql
update container_mapping set status='provisioning' where user_id='11111111-2222-3333-4444-555555555555';
```

重启 Gateway。预期日志显示 recovery，DB 中该行 status 变 `failed`，`error_message` 含 `ResourceNotFound`。

- [ ] **Step 14.8: 清理测试残留**

```bash
# 删用户行
# Supabase SQL Editor:
# delete from container_mapping where user_id='11111111-2222-3333-4444-555555555555';

# 删 Azure 文件 share
az storage share delete --name user-11111111 --account-name stlingxidev
```

- [ ] **Step 14.9: 记录验证结果**

在 `docs/superpowers/plans/` 下新建 `2026-05-30-phase-1.2-verification.md` 简记：

```markdown
# Phase 1.2 验证记录

- 验证时间：YYYY-MM-DD
- 验证人：
- 测试 user_id：11111111-...
- DoD 项：
  - [x] POST /api/purchase 立刻返 200 + provisioning
  - [x] GET /api/status 看到 6 步进度
  - [x] 1-3 分钟后变 ready
  - [x] Azure Portal 看到 Container App 创建
  - [x] 启动恢复：Succeeded 路径
  - [x] 启动恢复：失败路径

Phase 1.2 验收通过。可以开始 Phase 1.3。
```

```bash
git add docs/superpowers/plans/2026-05-30-phase-1.2-verification.md
git commit -m "docs: Phase 1.2 verification record"
```

---

## DoD（Phase 1.2 完成的判定标准）

全部以下 6 条 ✅，Phase 1.2 视为完成：

1. ✅ `pnpm -r test` 全 pass
2. ✅ `pnpm -r lint` 全无报错
3. ✅ POST /api/purchase 5 秒内返回 200 + `status='provisioning'`
4. ✅ GET /api/status 能观察到 6 步进度文案推进
5. ✅ 在 1-3 分钟内观察到 `status` 变 `ready`，Azure Portal 看到对应 Container App
6. ✅ 重启 Gateway 后启动恢复能正确处理 InProgress / Succeeded / Failed / NotFound 四种 Azure 状态

---

## 下一步（Phase 1.3 提示）

Phase 1.3 会基于 Phase 1.2 加：

- 微信开放平台 OAuth 登录（替换当前用 `x-user-id` header 的 mock auth）
- React + Vite 前端（macOS 桌面壳 + 三个 app）
- threads 表的 CRUD API
- `POST /api/chat/start` + `GET /api/chat/stream` Web 端实现（双层 stream_id 映射 + SSE 透传到浏览器）
- onboarding 进度页

待 1.2 验收通过、Hermes Image（Phase 1.1）可获取后再启 Phase 1.3 plan。
