# Phase 1.2 本地验证记录

- 验证时间：2026-05-30
- 验证环境：macOS Darwin 25.5.0, Node 24, pnpm 9, Supabase CLI 2.75, Docker 29.1
- 测试 user_id：`11111111-2222-3333-4444-555555555555`

## DoD 项

- [x] `pnpm -r test` — 15/15 tests pass (1 healthz + 4 cache + 2 manager + 2 status + 2 purchase + 3 recovery + 1 local provisioner)
- [x] `pnpm -r lint` — `tsc --noEmit` 全无报错
- [x] 本地 Supabase 起步成功（`supabase start` with project_id=laifu）
- [x] 5 张表全部创建：users / container_mapping / wechat_sessions / context_tokens / threads
- [x] `POST /api/purchase` < 1 秒返回 `{status: 'provisioning'}`
- [x] `GET /api/status` 轮询观察到所有 6 步进度：5% → 20% → 40% → 70% → 90% → 100%/ready
- [x] 最终 `container_url='http://localhost:8080'`，`container_name='hermes-11111111'`
- [x] 重复购买幂等：第二次 POST /api/purchase 直接返回当前 status='ready'，不重复 provisioning
- [x] 本地模式启动**不**跑 recoverProvisioning（log 不含 "recovering stuck provisioning rows"）

## 关键修复

- **`fix(gateway): refresh cache after each provisioning step`** (commit `04135c7`)
  - Bug: provisioner 中间 5 步只写 DB 不刷 cache，`/api/status` 读 cache 拿不到进度
  - Fix: 每步 update 后 select-and-set cache，让前端轮询能看到实时推进

## E2E 实跑日志（500ms 间隔轮询）

```
[poll 1] provisioning | 5%   | 正在创建账户与订单
[poll 2] provisioning | 5%   | 正在创建账户与订单
[poll 3] provisioning | 20%  | 正在生成数字助理实例
[poll 4] provisioning | 40%  | 为助理分配 DID 与 Agent 运行时
[poll 5] provisioning | 40%  | 为助理分配 DID 与 Agent 运行时
[poll 6] provisioning | 70%  | 初始化默认能力
[poll 7] provisioning | 90%  | 装载基础知识库
[poll 8] provisioning | 90%  | 装载基础知识库
[poll 9] ready        | 100% | 灵犀助理上岗完成
```

约 4 秒走完 6 步 + ready，符合 6 × 800ms stub 延迟设定。

## 已知问题 / 后续

- **FK 约束**：`container_mapping.user_id` 引用 `users(id)`。Phase 1.2 本地测试需手工插一行 users（Phase 1.3 接微信 OAuth 后自动）
- **Hermes Image 占位**：当前 `localContainerUrl=http://localhost:8080` 是 stub，Phase 1.1 同事交付真实 Hermes Image 后 `docker run` 起在该端口即可端到端聊天
- **Azure 部署**：Bicep 模板已就绪但未应用，Phase 1.5 执行

## 验收结论

**Phase 1.2 本地验收通过。** 可以进入 Phase 1.3（Web 前端 + 微信 OAuth + threads + Web 聊天 SSE 透传）。
