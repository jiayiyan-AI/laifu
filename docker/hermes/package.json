{
  "name": "lingxi-hermes-container",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes 容器源码 (Bun + TypeScript). 这里的 package.json **仅用于本地 LSP / typecheck 类型解析**, 镜像 build 时通过 .dockerignore 排除 node_modules — 容器内 Bun 直接跑 .ts 不需要任何 package.json。",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
